@*@model IEnumerable<Client.Models.EmployeeLevel>

@{
    ViewData["Title"] = "ListEmployeeLevel";
}

<h1>ListEmployeeLevel</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th hidden>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Level)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Allowence)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td hidden>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Level)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Allowence)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
*@
@model IEnumerable<Client.Models.EmployeeLevel>

@{
    ViewData["Title"] = "ListEmployeeLevel";
    Layout = "_SidebarLayout";
}

<div id="main">
    <header class="mb-3 ">
        <a href="#" class="burger-btn d-block d-xl-none">
            <i class="bi bi-justify fs-3"></i>
        </a>
    </header>

    <div class="page-heading">
        <div class="page-title">
            <div class="row">
                <div class="col-12 col-md-6 order-md-1 order-last">
                    <h3>List Employee Level</h3>
                    <p class="text-subtitle text-muted">
                        Manager Employee Level Data
                    </p>
                </div>
                <div class="col-12 col-md-6 order-md-2 order-first">
                    <nav aria-label="breadcrumb"
                         class="breadcrumb-header float-start float-lg-end">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item">
                                <a href="index.html">Dashboard</a>
                            </li>
                            <li class="breadcrumb-item active" aria-current="page">
                                Employee Level
                            </li>
                        </ol>
                    </nav>
                </div>
            </div>
        </div>
        <section class="section">
            <div class="card">
                <div class="">
                    </br><a href="@Url.Action("CreateEmployeeLevel","Admin")" class="btn btn-primary float-end me-4" data-toggle="modal" data-target="#modalTambah">
                        <div class="d-inline-block me-2 "><i class="bi bi-plus-lg"></i></div> Employee Level
                    </a>

                    
                </div>

                <div class="card-body">
                    <table class="table table-striped" id="table1">
                        <thead>
                            <tr>
                                <th style="display: none">ID</th>
                                <th>Title</th>
                                <th>Level</th>
                                <th>Salary</th>
                                <th>Allowence</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td hidden>
                                        @Html.DisplayFor(modelItem => item.Id)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Title)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Level)
                                    </td>
                                    <td>
                                        Rp @Html.Raw(string.Format("{0:N0}", item.Salary))
                                    </td>
                                    <td>
                                        Rp @Html.Raw(string.Format("{0:N0}", item.Allowence))
                                    </td>
                                    

                                    <td>
                                        <a href="@Url.Action("EditEmployeeLevel", new { id = item.Id })" class="btn btn-warning" data-toggle="tooltip" data-placement="top" title="Edit">
                                            <i class="bi bi-pencil-fill"></i>
                                        </a>
                                        <a href="@Url.Action("DeleteEmployeeLevel", new { id = item.Id })" class="btn btn-danger" data-toggle="tooltip" data-placement="top" title="Delete">
                                            <i class="bi bi-trash3-fill"></i>
                                        </a>

                                        @*<a href="@Url.Action("EditEmployeeLevel", new { id = item.Id })" class="btn btn-success">Edit</a>*@
                                        @*<a href="@Url.Action("DeleteEmployeeLevel", new { id = item.Id })" class="btn btn-danger">Delete</a>*@
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </section>
    </div>

    <footer>
        <div class="footer clearfix mb-0 text-muted mt-auto px-4">
            <div class="float-start text-sm">
                <p>2023 &copy;</p>
            </div>
            <div class="float-end text-sm">
                <p>
                    Project MCC by
                    <span class="text-danger">
                        <i class="bi bi-heart-fill icon-mid"></i>
                    </span>
                    <a href="">Kelompok 4</a>
                </p>
            </div>
        </div>
    </footer>
</div>





